# --- CDSYNC CONFIGURATION ---

# Rclone Remote Name (e.g., gdrive:, my-remote:)
# Must match the name configured in 'rclone config'
# Change 'my-remote' accordingly below.
RCLONE_REMOTE="my-remote:"

# Absolute path to the local directory to be synchronized - Change 'my-remote' accordingly below.
# This directory must exist. 
# Change 'my-remote' accordingly below.
LOCAL_SYNC_DIR="$HOME/my-remote"

# Lock file path (to prevent concurrent executions for this specific sync)
# Change 'my-remote' accordingly below.
LOCK_FILE="/tmp/cdsync_my-remote.lock"

# Custom Log File Path
# Leave empty to use the default: ./cdsync.log in the script directory
CUSTOM_LOG_FILE=""

# --- NEW CONFIGURATIONS ---

# Rclone Config Path
# Optional: Specify a custom path for the rclone configuration file.
# Default: $HOME/.config/rclone/rclone.conf
# RCLONE_CONFIG_PATH="/path/to/my/rclone.conf"

# Notification Settings
# Level 0: OFF (Silent)
# Level 1: ERROR (Critical errors only)
# Level 2: ALL (Default - Success/Start notifications are transient)
NOTIFY_LEVEL=2

# Polling Interval (in minutes)
# How often to check for remote changes. Default: 5
POLL_INTERVAL=5

# Force Sync Newer Files
# If enabled, conflicts in bisync will overwrite older files with newer files
# without creating conflict copies, bypassing data safety but maintaining a clean sync
# Default value: false
FORCE_SYNC_NEWER=false

# Select Notification Level
# Level 0: OFF (Silent)
# Level 1: ERROR (Critical errors only)
# Level 2: ALL (Default - Success/Start notifications are transient)
NOTIFY_LEVEL=2